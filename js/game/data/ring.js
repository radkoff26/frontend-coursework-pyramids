export default class Ring {
    id
    width
    left
    top
    colorHex

    constructor(id, width, left, top, colorHex) {
        this.id = id
        this.width = width
        this.left = left
        this.top = top
        this.colorHex = colorHex
    }
}